[phases.setup]
nixpkgs = ["nodejs_20", "gcc", "g++", "openjdk", "python3"]

[phases.build]
cmds = ["cd backend && npm install", "cd backend && npm run build"]

[phases.start]
cmd = "cd backend && npm run start"
